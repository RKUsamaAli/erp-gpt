# .NET
bin/
obj/
*.user
appsettings.Development.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Vector DB / models
qdrant_storage/
*.gguf

# Dataset — 19 MB, distributed via the data-v1 GitHub release, not git
adventureworks.dump

# Logs (step 13 output — local only until we decide retention)
agent/logs/
*.jsonl

# Editors / OS
.vs/
.vscode/
.idea/
.DS_Store

# Secrets — never commit
*.env
.env*
secrets.json
