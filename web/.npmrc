# Makes the "engines" range in package.json a hard failure rather than a warning.
# Node 16 is still installed on at least one machine here, and `nvm use` repoints
# a single global symlink — so the wrong Node is one command away. Failing at
# `npm install` with a readable message beats a confusing build error later.
engine-strict=true
